#include "/Engine/Public/Platform.ush"

RWStructuredBuffer<float> ClothData;

[numthreads(64, 1, 1)]
void MainCS(uint3 ThreadId : SV_DispatchThreadID)
{
    uint Index = ThreadId.x;
    ClothData[Index] = ClothData[Index] * 2.0f;
}